--0902 문제풀이
10. order by 절에서는 ','를 통해서 정렬을 두 번 할 수 있다.
15. 오라클에서 제공하는 내장함수 'lower' 를 통해 해결
함수는    1) 특정기능을 제공, 
	2) 내가만드는 경우도 있고, 내장함수를 사용하는 경우
	3) 사용할 때, 콜할 때 값을 넘겨줄수 있는 기능을 가진 것은 매개변수
	   함수는 리턴값을 가짐
	
내장함수 "lower" : 오라클이 제공하는 내장함수이며, 알파벳 대문자를 소문자로 바꿔주는 기능을 가진다.
		lower()로 함수를 call하며, 매개변수에 문자열을 넣어주면 소문자로 바뀐 문자열을 리턴한다. 
		ex) select lower('ABCD') from dual; >>'abcd' 출력 
16. substr() 함수 검색
매개변수: 3개>> 매개변수별 의미파악>>간단한 예제까지만 확인


--group by
**튜플을 선정한 다음 동작하는 명령
보통 그룹별 통계를 낼 때 사용됨(각 튜플의 내용은 포함x)

*연구과제*
그룹을 만들 때 그룹별 조건을 지정할 수 있다.
예를 들어, 그룹의 인원이 3명 이상인 그룹의 인원수를 출력하시오
이 지시사항은 그룹별로 인원을 카운팅하고, 
그룹 중에 인원수가 3명 이상인 그룹만 선택하는 의미이다.
그룹에 조건을 지정하는 방법을 찾아보세요.

>>having: 그룹화 할때의 조건 // where: 튜플을 선별할 때의 조건
**havaing의 조건인지 where의 조건인지 명확하게 구별
	집계 함수는 having 절과 함께 사용할 수 있다.
	Having은 그룹화 또는 집계가 발생한 후 레코드를 필터링하는데 사용된다.
	Where은 그룹화 또는 집계가 발생하기 전에 레코드를 필터링하는데 사용된다.

다음 문제를 쿼리문으로 완성하세요
-그룹의 인원이 3명 이상인 그룹의 인원수를 출력하시오

select count(*)
from users
group by grade 
having count(*)>=3;

-전체평균보다 높은 평균을 가진 그룹, 점수 출력하시오
select grade,avg(point)
from users
group by grade 
having avg(point)>=??;
	&&
??=select avg(point) from users;

>>select grade,avg(point)
from users
group by grade 
having avg(point)>=
(select avg(point) from users);  //서브쿼리
서브쿼리의 결과값이 전체평균임. 그 결과값으로 그룹의 조건을 완성
avg(point)>=서브쿼리 결과값(전체평균)


서브쿼리는 본쿼리 안에 소괄호로 시작하는 select문
서브쿼리의 결과값을 본쿼리에 활용한다.
서브쿼리는 본쿼리의 조건으로 활용이 가능-where절, having절
본쿼리의 테이블로 활용가능-from 절
본쿼리의 원하는 컬럼에서 활용가능-select절

정리: select절에서 서브쿼리를 활용하면 >> 스칼라 서브쿼리
from절에서 서브쿼리를 활용하면 >> 인라인뷰 서브쿼리

select m.name,m.addr
from (select*from users) m;

select m.name,m.addr
from (select*from users) m
where m.grade=3;

